Description:
    Generator to create initializers and migrations for Galleriable engine in your app.

Example:
    rails generate galleriable:install

    This will create:
      a route to the galleriable engine in your application